require 'spec_helper'

describe Motion::Rest do
  it 'has a version number' do
    expect(Motion::Rest::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
